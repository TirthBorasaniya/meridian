# Meridian: Agentic RAG System
# Python 3.11 only. Install into an active virtualenv:
#   python3.11 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt

# --- Graph orchestration ---
langgraph>=0.2.40,<0.3
langgraph-checkpoint-sqlite>=2.0.0

# --- LangChain core and integrations ---
langchain-core>=0.3.0,<0.4
langchain-groq>=0.2.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# --- Vector store and sparse retrieval ---
qdrant-client>=1.11.0
rank-bm25>=0.2.2

# --- Embeddings and reranking ---
sentence-transformers>=3.0.0
transformers>=4.44.0
torch>=2.2.0

# --- LLM inference ---
groq>=0.11.0

# --- Observability ---
langfuse>=2.50.0,<3.0

# --- Web fallback ---
tavily-python>=0.5.0

# --- Evaluation ---
ragas>=0.2.0
datasets>=2.20.0

# --- Ingestion pipeline ---
prefect>=2.19,<3.0
# Pinned: anyio>=4.5 breaks prefect 2.x's GatherTaskGroup (abstract method
# mismatch with TaskGroup.create_task). See CLAUDE.md Breaking API Changes.
anyio==4.4.0
docling>=2.0.0

# --- Metadata store ---
SQLAlchemy>=2.0.30
psycopg[binary]>=3.2.0

# --- arXiv API ---
arxiv>=2.1.0

# --- Serving ---
fastapi>=0.111,<0.112
uvicorn[standard]>=0.30.0

# --- Configuration and validation ---
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Utilities ---
numpy>=1.26,<2.0
tenacity>=8.5.0
httpx>=0.27.0

# --- The Meridian package itself (editable), so `import meridian` resolves
#     after `pip install -r requirements.txt` without a separate step. ---
-e .
